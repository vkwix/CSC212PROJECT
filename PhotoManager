public class PhotoManager {

public class PhotoManager {
	private LinkedList<photos> photos;
	public PhotoManager() {
		  photos = new LinkedList<Photo>()
	}
	public LinkedList<Photo> getPhotos(){
		return photos;
	}
	public void addPhoto(Photo p) {
		photos.findFirst();
		if (photos.empty()) {
			photos.insert(p);
		}
		else {
			while(!photos.last()) {
				photos.findNext();
			}
			photos.insert(p);
		}
		
	}
	public void deletePhoto(String path) {
		if(photos.empty())
			return;
		Photo currentPhoto = photos.retrieve;
		while(true) {
			if(current.getpath().equals(path)) {
				photos.remove();
				break;
			}
			if(photos.last())
				break
			photos.findNext();	
		}
	}
	}
